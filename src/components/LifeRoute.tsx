import { lifeRoute } from '../data/lifeRoute'; import { ArrowDownRight } from 'lucide-react'
export function LifeRoute() { return <section id="route" className="section"><div className="section-heading"><p className="eyebrow">04 · 人生路线</p><h2>从山阴到西湖：张岱人生路线</h2><p>一条由游赏转向回望的精神地理。</p></div><div className="route-line">{lifeRoute.map((point, i) => <article key={point.place} className="route-item"><div className="route-dot"><span>{i + 1}</span></div><div className="route-text"><p className="tag">{point.role}</p><h3>{point.place}</h3><p>{point.description}</p><em>{point.visual}</em></div>{i < lifeRoute.length - 1 && <ArrowDownRight className="route-arrow" size={18}/>}</article>)}</div></section> }
