import { historyComparison } from '../data/historyComparison'
export function HistoryComparison() { return <section id="history" className="section history"><div className="section-heading"><p className="eyebrow">05 · 时代对照</p><h2>个人命运与时代崩塌</h2></div><div className="history-head"><span>明代历史</span><span>张岱人生 / 作品</span></div><div className="history-list">{historyComparison.map((item, index) => <article key={`${item.year}-${index}`} className="history-row"><div><p className="year">{item.year}</p><p>{item.history}</p></div><i></i><div><p>{item.zhangDai}</p><em>{item.meaning}</em></div></article>)}</div></section> }
