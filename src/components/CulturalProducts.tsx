import { culturalProducts } from '../data/culturalProducts'; import { ArrowUpRight } from 'lucide-react'
const symbols = ['册', '亭', '舟', '问'];
export function CulturalProducts() { return <section id="products" className="section"><div className="section-heading"><p className="eyebrow">07 · 文创转化</p><h2>把这一夜带走</h2><p>从文本到器物，从旧梦到日常</p></div><div className="product-grid">{culturalProducts.map((product, i) => <article className="product" key={product.name}><div className={`product-visual p${i}`}><span>{symbols[i]}</span></div><div className="product-body"><p className="tag">{product.type}</p><h3>{product.name}</h3><p>{product.description}</p><div className="features">{product.features.map(f => <span key={f}>{f}</span>)}</div><ArrowUpRight size={18}/></div></article>)}</div></section> }
