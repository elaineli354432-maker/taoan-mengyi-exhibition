import { MapPin } from 'lucide-react'; import { lifeTimeline } from '../data/lifeTimeline'
export function LifeTimeline() { return <section id="life" className="section"><div className="section-heading"><p className="eyebrow">02 · 生平卷轴</p><h2>一生如梦：张岱生平时间线</h2><p>点击年份，翻阅一则人生札记。</p></div><div className="life-scroll">{lifeTimeline.map((item, i) => <article className="life-card paper" key={item.title}><span className="number">0{i + 1}</span><p className="year">{item.year}</p><p className="period">{item.period}</p><h3>{item.title}</h3><p className="location"><MapPin size={13}/>{item.location}</p><p>{item.description}</p><em>{item.theme}</em></article>)}</div></section> }
